describe("Proximos passos -> auto refetching polling realtime", () => {
   it("Deve renderizar o titulo", () => {
      throw "Auto refetching polling realtime";
   });

   it.todo("Deve renderizar o paragrafo");

   describe("Deve renderizar o label Query Interval speed (ms): corretamente", () => {
      it.todo("Deve renderizar o title");

      it.todo("Deve renderizar o input number:step");

      it.todo("Deve renderizar o span indicador piscando");
   });

   it.todo("Deve renderizar o titulo TODO list");

   it.todo("Deve renderizar o form com input corretamente");

   it.todo("Deve renderizar a lista");

   it.todo("Deve renderizar o botao de Clear All");
});
